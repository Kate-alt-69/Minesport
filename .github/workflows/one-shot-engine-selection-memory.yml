name: One Shot Engine Selection Memory

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/one-shot-engine-selection-memory.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: minesport-engine-selection-memory
  cancel-in-progress: false

jobs:
  integrate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Sync latest main
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          git checkout -B main origin/main

      - name: Patch region-local selection filtering
        run: python .github/engine_selection_memory_patch.py

      - name: Remove one-shot integration files
        shell: bash
        run: |
          rm -f .github/engine_selection_memory_patch.py
          rm -f .github/workflows/one-shot-engine-selection-memory.yml

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '22'

      - uses: gradle/actions/setup-gradle@v6

      - name: Validate engine patch
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          grep -Fq 'filterDecodedSelection(' engine/src/main/java/dev/kastrick/minesport/IpcMode.java
          grep -Fq 'decodedBlockCount += contents.blocks().size();' engine/src/main/java/dev/kastrick/minesport/IpcMode.java
          grep -Fq 'previewDecodedBlocks += regionBlocks.size();' engine/src/main/java/dev/kastrick/minesport/IpcMode.java
          ! grep -Fq 'allBlocks.removeIf(block -> !exact.contains' engine/src/main/java/dev/kastrick/minesport/IpcMode.java
          grep -Fq 'Control implemented:' doc/engine-runtime-audit.md

      - name: Test Java engine
        working-directory: engine
        shell: bash
        run: |
          set -euo pipefail
          chmod +x gradlew
          ./gradlew --no-daemon --stacktrace clean build

      - name: Commit tested fix with collision-free ID
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git stash push -u -m engine-selection-memory-tested
          git pull --rebase origin main
          git stash pop

          max="$(git log origin/main --format=%s | sed -nE 's/.*BUG-([0-9]+).*/\1/p' | sort -n | tail -1)"
          if [ -z "$max" ]; then max=0; fi
          next=$((max + 1))

          git add engine/src/main/java/dev/kastrick/minesport/IpcMode.java doc/engine-runtime-audit.md .github/engine_selection_memory_patch.py .github/workflows/one-shot-engine-selection-memory.yml
          git diff --cached --check
          git commit -m "FIX | BUG-$next Filter sparse engine selections per region"
          git push origin HEAD:main
