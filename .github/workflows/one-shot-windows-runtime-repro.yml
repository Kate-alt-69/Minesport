name: One Shot Windows Runtime Repro Fixes

on:
  push:
    branches: [main]

permissions:
  contents: write
  actions: read

concurrency:
  group: one-shot-windows-runtime-repro
  cancel-in-progress: false

jobs:
  integrate:
    if: github.event.head_commit.message == 'CHORE | stage Windows runtime repro fixes'
    runs-on: windows-latest
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.92.0
          components: rustfmt
      - name: Restore embedded engine artifact
        uses: actions/download-artifact@v7
        with:
          name: minesport-engine-ci
          path: engine/build/libs
          run-id: 33797953559
          github-token: ${{ github.token }}
      - name: Restore embedded loader bridges
        uses: actions/download-artifact@v7
        with:
          name: loader-bridges-1.21.10
          path: dist/bundled-export-worker
          run-id: 33797953559
          github-token: ${{ github.token }}
      - name: Validate reproduced Windows and CI fixes
        shell: bash
        env:
          MINESPORT_FAST_CHECK: '1'
        run: |
          set -euo pipefail
          python .github/patches/apply-windows-runtime-repro-fixes.py
          rustfmt --edition 2024 desktop/src/runtime_worker.rs desktop/src/engine_update.rs desktop/src/main.rs
          git diff --check
          cargo test --locked --manifest-path desktop/Cargo.toml runtime_worker::tests::direct_launch_profiles_preserve_loom_bootstrap_arguments
          cargo test --locked --manifest-path desktop/Cargo.toml engine_update::tests::updater_helper_is_isolated_and_has_network_stack_headroom
          cargo check --locked --release --manifest-path desktop/Cargo.toml --all-targets
          grep -Fq 'const DIRECT_LAUNCH_PROFILE_SCHEMA: u32 = 2;' desktop/src/runtime_worker.rs
          grep -Fq 'runTask.jvmArgumentProviders.collectMany' desktop/src/runtime_worker.rs
          grep -Fq 'direct_runtime_profile_has_loader_bootstrap' desktop/src/runtime_worker.rs
          grep -Fq 'handle_background_check_mode' desktop/src/main.rs
          grep -Fq '.creation_flags(CREATE_NO_WINDOW)' desktop/src/engine_update.rs
          grep -Fq '.stack_size(ENGINE_UPDATE_THREAD_STACK)' desktop/src/engine_update.rs
          grep -Fq 'if: always() && !cancelled()' .github/workflows/build.yml
      - name: Publish rebased fixes
        shell: bash
        env:
          MINESPORT_FAST_CHECK: '1'
        run: |
          set -euo pipefail
          git reset --hard HEAD
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git pull --rebase origin main
          python .github/patches/apply-windows-runtime-repro-fixes.py
          rustfmt --edition 2024 desktop/src/runtime_worker.rs desktop/src/engine_update.rs desktop/src/main.rs
          git diff --check
          cargo test --locked --manifest-path desktop/Cargo.toml runtime_worker::tests::direct_launch_profiles_preserve_loom_bootstrap_arguments
          cargo test --locked --manifest-path desktop/Cargo.toml engine_update::tests::updater_helper_is_isolated_and_has_network_stack_headroom
          cargo check --locked --release --manifest-path desktop/Cargo.toml --all-targets

          subjects="$(git log origin/main --format=%s)"
          max="$(printf '%s\n' "$subjects" | grep -oE 'BUG-[0-9]+' | cut -d- -f2 | sort -n | tail -1)"
          launch_bug=$(( ${max:-0} + 1 ))
          updater_bug=$(( launch_bug + 1 ))
          ci_bug=$(( updater_bug + 1 ))

          git add desktop/src/runtime_worker.rs
          git diff --cached --check
          git commit -m "FIX | BUG-${launch_bug} Preserve Loom direct-launch bootstrap arguments"

          git add desktop/src/engine_update.rs desktop/src/main.rs
          git diff --cached --check
          git commit -m "FIX | BUG-${updater_bug} Isolate engine update discovery from Workbench"

          git add .github/workflows/build.yml
          git rm .github/patches/apply-windows-runtime-repro-fixes.py
          git diff --cached --check
          git commit -m "FIX | BUG-${ci_bug} Ignore superseded CI cancellations"

          git restore --staged --worktree .
          git pull --rebase origin main
          cargo test --locked --manifest-path desktop/Cargo.toml runtime_worker::tests::direct_launch_profiles_preserve_loom_bootstrap_arguments
          cargo test --locked --manifest-path desktop/Cargo.toml engine_update::tests::updater_helper_is_isolated_and_has_network_stack_headroom
          cargo check --locked --release --manifest-path desktop/Cargo.toml --all-targets
          grep -Fq 'if: always() && !cancelled()' .github/workflows/build.yml
          git push origin HEAD:main
