name: One Shot BUG-001 Backend Recovery

on:
  push:
    branches:
      - main
    paths:
      - ".github/bug_001_backend_recovery.py"
      - ".github/workflows/one-shot-bug-001-backend-recovery.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply source patch
        run: |
          python3 -m py_compile .github/bug_001_backend_recovery.py
          python3 .github/bug_001_backend_recovery.py
          git diff --check

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.92.0

      - name: Install Slint build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libfontconfig1-dev \
            libfreetype6-dev \
            libxcb1-dev \
            libxcb-render0-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-dev

      - name: Test Rust desktop
        working-directory: desktop
        env:
          MINESPORT_FAST_CHECK: "1"
        run: |
          cargo check --locked --all-targets
          cargo test --locked --all-targets

      - name: Commit tested source fix and remove one-shot gate
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add desktop/src/ipc.rs desktop/src/app.rs
          git rm \
            .github/bug_001_backend_recovery.py \
            .github/workflows/one-shot-bug-001-backend-recovery.yml
          git diff --cached --check
          git commit -m "FIX | BUG-001 Recover backend after IPC death"
          git push origin HEAD:main
