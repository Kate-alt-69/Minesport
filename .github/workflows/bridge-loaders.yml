name: Minesport Loader Bridges

on:
  push:
    branches: [main]
    paths:
      - 'bridge/**'
      - 'bridge-versions/**'
      - 'bridge-forge/**'
      - 'bridge-forge-versions/**'
      - 'bridge-neoforge/**'
      - 'bridge-neoforge-versions/**'
      - 'bridge-quilt/**'
      - 'bridge-quilt-versions/**'
      - 'desktop/src/bin/bridge-prepare.rs'
      - 'desktop/src/bridge_compat.rs'
      - 'desktop/src/bridge_family.rs'
      - 'desktop/src/bridge_build.rs'
      - 'desktop/src/bridge_java.rs'
      - 'desktop/src/toolchain.rs'
      - '.github/workflows/bridge-loaders.yml'
  workflow_dispatch:

jobs:
  canonical-1-21-10:
    name: ${{ matrix.name }} Bridge · Minecraft 1.21.10
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Forge
            slug: forge
            project: bridge-forge
          - name: NeoForge
            slug: neoforge
            project: bridge-neoforge
          - name: Quilt
            slug: quilt
            project: bridge-quilt
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Build canonical bridge
        working-directory: ${{ matrix.project }}
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon --console=plain build
      - name: Verify bridge JAR
        working-directory: ${{ matrix.project }}
        run: |
          test -n "$(find build/libs -maxdepth 1 -type f -name '*.jar' ! -name '*-sources.jar' ! -name '*-javadoc.jar' -print -quit)"
          find build/libs -maxdepth 1 -type f -name '*.jar' ! -name '*-sources.jar' ! -name '*-javadoc.jar' -print
      - name: Upload bridge JAR
        uses: actions/upload-artifact@v4
        with:
          name: minesport-bridge-${{ matrix.slug }}-1.21.10
          path: |
            ${{ matrix.project }}/build/libs/*.jar
            !${{ matrix.project }}/build/libs/*-sources.jar
            !${{ matrix.project }}/build/libs/*-javadoc.jar
          if-no-files-found: error
          retention-days: 14

  compatibility-floor-1-21-5:
    name: ${{ matrix.name }} Bridge · PATCH floor 1.21.5
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - name: Fabric
            slug: fabric
            jar: minesport-bridge-1.21.5.jar
          - name: Forge
            slug: forge
            jar: minesport-bridge-forge-1.21.5.jar
          - name: NeoForge
            slug: neoforge
            jar: minesport-bridge-neoforge-1.21.5.jar
          - name: Quilt
            slug: quilt
            jar: minesport-bridge-quilt-1.21.5.jar
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.92.0
      - uses: gradle/actions/setup-gradle@v4
      - name: Install Rust/Slint build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            pkg-config libfontconfig1-dev libfreetype6-dev libx11-dev libxcursor-dev \
            libxrandr-dev libxi-dev libgl1-mesa-dev libwayland-dev libxkbcommon-dev
      - name: Generate and compile 1.21.5 Bridge with Rust patcher
        env:
          MINESPORT_HEADLESS_BRIDGE_PREPARE: '1'
        run: |
          cargo run --manifest-path desktop/Cargo.toml --bin bridge-prepare -- \
            --loader '${{ matrix.slug }}' \
            --version '1.21.5' \
            --output dist/loader-floor
          test -s 'dist/loader-floor/${{ matrix.jar }}'
      - name: Upload 1.21.5 Bridge
        uses: actions/upload-artifact@v4
        with:
          name: minesport-bridge-${{ matrix.slug }}-1.21.5
          path: dist/loader-floor/${{ matrix.jar }}
          if-no-files-found: error
          retention-days: 14
