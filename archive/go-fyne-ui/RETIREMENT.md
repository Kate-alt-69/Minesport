# Retirement status

The Fyne desktop UI is archived and no longer part of the active Minesport product build. The active desktop is `/desktop` (Rust + Slint).

The archive is retained only to compare behavior during the 0.2.0 migration. New UI work belongs in Slint/Rust.
