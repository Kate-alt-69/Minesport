{
  "schemaVersion": 1,
  "id": "minesport_bridge",
  "version": "${version}",
  "name": "Minesport Bridge",
  "description": "Minesport runtime-registry bridge — captures registered block states, baked geometry, texture identifiers and light metadata through an isolated client worker, then exits. Not intended for normal gameplay.",
  "authors": ["Kastrick"],
  "environment": "client",
  "entrypoints": {
    "client": ["dev.kastrick.minesport.bridge.MinesportBridge"]
  },
  "depends": {
    "fabricloader": ">=0.18.5",
    "fabric-api": "*",
    "minecraft": ">=1.21.9 <=1.21.10",
    "java": ">=21"
  },
  "suggests": {
    "polymer": "*"
  }
}
