# Retired Go/Fyne UI

The active Minesport desktop application is now implemented in Rust + Slint under `/desktop`.

The complete former Go/Fyne UI, including its tests, is preserved under `/archive/go-fyne-ui/ui` for migration/reference purposes.

Do not add new UI code here.
